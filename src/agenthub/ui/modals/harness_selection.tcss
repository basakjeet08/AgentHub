HarnessSelectionModal {
    align: center middle;
    background: $background 70%;
}

#harness-selection-dialog {
    width: 54;
    max-width: 90%;
    height: auto;
    max-height: 80%;
    padding: 1 2;
    background: $surface;
    border: tall $panel;
}

#harness-selection-header {
    width: 100%;
    height: 2;
}

#harness-selection-title {
    width: 1fr;
    height: 1;
    color: $foreground;
    text-style: bold;
}

#harness-selection-list {
    width: 100%;
    height: auto;
    min-height: 3;
    max-height: 12;
    background: transparent;
    border: none;
}

#harness-selection-list > .option-list--option {
    padding: 0 1;
}

#harness-selection-list > .option-list--option-highlighted {
    background: $panel;
    color: $foreground;
    text-style: bold;
}

#harness-selection-help {
    width: 100%;
    height: 2;
    margin-top: 1;
}
